# Secret credentials — copy application_config.h.example and fill in your values
IOT_SDK/config/application_config.h

# TLS certificates and private keys (never commit these)
*.pem
*.key
*.crt
*.der
certs/
certificates/

# STM32CubeIDE build output
Debug/
Release/

# IDE metadata (optional — remove these lines if you want to commit IDE settings)
.metadata/
.settings/

# STM32CubeMX backup and generated metadata
*.bak
.mxproject

# Other
CLAUDE.MD
notes.MD